Homepage proof screenshot: hero.png (referenced by index.njk "See it" section).
~1600x1000 (16:10). Currently a real Parachute surface (UniSurface) with the
quick-capture panel open, captured signed-in. Until a file is present the
homepage shows a styled placeholder frame (no broken-image icon).
