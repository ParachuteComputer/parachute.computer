---
layout: base.njk
title: "Deploy to a VPS"
description: "Run Parachute always-on on a Linux box you own — Hetzner, DigitalOcean, or any VPS. The same one-command, bun-native install, on a machine that never sleeps."
permalink: /deploy/vps/
---
<main>

    <section class="hero">
        <h1 class="fade-up fade-up-2">Deploy to a VPS</h1>
        <p class="tagline fade-up fade-up-3">Hetzner, DigitalOcean, or any Linux box. The same one-command install, on a machine that never sleeps.</p>
    </section>

    <section class="section">
        <h2>Why this path</h2>

        <p>A laptop sleeps; a sleeping hub can't answer your phone or your AI's connector. A small always-on Linux box fixes that without handing your data to anyone &mdash; you rent the box, you own everything on it.</p>

        <p><strong>This is the bun-native happy path.</strong> The exact same <a href="/start/">install flow</a> you'd run locally &mdash; <code>bun add -g @openparachute/hub &amp;&amp; parachute init</code> &mdash; on a server that never sleeps. No Dockerfile to maintain, no container build to keep in sync with releases. The hub installs itself as a <code>systemd</code> unit (survives reboots and crashes), supervises every module as a child, and <code>parachute upgrade</code> keeps the stack current.</p>

        <p>Hetzner and DigitalOcean are worked through below, but any Ubuntu VPS &mdash; EC2, Linode, Vultr, a box in your closet &mdash; walks the identical steps.</p>
    </section>

    <section class="section" id="the-flow">
        <h2>The flow</h2>

        <p>Six steps from a fresh box to a hub your AI can reach. The provider-specific bit is just step 1 (provision); everything from step 2 on is identical regardless of where the box lives.</p>

        <h3>1. Provision a small Ubuntu box</h3>
        <p>Smallest shared-CPU instance with <strong>~2GB RAM</strong> (1GB works for hub + vault alone; 2GB is comfortable for vault + surface + scribe). Image <strong>Ubuntu 24.04 LTS</strong>. Add your SSH public key at creation. Note the public IPv4. The <a href="#hetzner">Hetzner</a> and <a href="#digitalocean">DigitalOcean</a> worked examples below cover the click-path on each.</p>

        <h3>2. SSH in</h3>
        <div class="code-inline">
            <code>ssh root@&lt;your-box-ip&gt;</code>
        </div>
        <p>Or your non-root user, if you created one.</p>

        <h3>3. Install Bun 1.3+</h3>
        <div class="code-inline">
            <code>curl -fsSL https://bun.sh/install | bash</code>
        </div>
        <p>Then open a fresh shell and confirm <code>bun --version</code> is <strong>1.3 or newer</strong> (a fresh shell puts <code>~/.bun/bin</code> on your PATH).</p>

        <h3>4. Install + init</h3>
        <div class="code-inline">
            <code>bun add -g @openparachute/hub &amp;&amp; parachute init</code>
        </div>
        <p>This installs the <code>parachute</code> binary, installs and starts the hub as a <code>systemd</code> unit on <strong>:1939</strong> (survives reboots), installs the vault, and &mdash; because you're on an SSH session &mdash; pre-selects public exposure so the wizard URL is reachable from your laptop.</p>

        <h3>5. Finish the wizard</h3>
        <p>Two ways, your choice:</p>
        <ul>
            <li><strong>Headless</strong> &mdash; <code>parachute init --cli-wizard</code> walks Account &rarr; Vault &rarr; Expose right there in the terminal.</li>
            <li><strong>Browser</strong> &mdash; open the <code>/admin/setup</code> URL that <code>init</code> prints, in your laptop browser. Paste the one-time bootstrap token <code>init</code> shows (it prints the token when it exposes publicly).</li>
        </ul>

        <h3>6. Clean public HTTPS URL</h3>
        <div class="code-inline">
            <code>parachute expose public --cloudflare --domain vault.example.com</code>
        </div>
        <p>A Cloudflare Tunnel on a domain you own &mdash; this is what claude.ai's connector needs. The done screen hands you a copy-paste <code>claude mcp add</code> command with a fresh token; point any other client at <code>&lt;hub-origin&gt;/vault/&lt;name&gt;/mcp</code>.</p>
    </section>

    <section class="section" id="hetzner">
        <h2>Hetzner — worked example</h2>

        <p>Hetzner Cloud (<a href="https://console.hetzner.cloud" target="_blank" rel="noopener">console.hetzner.cloud</a>) is the cheapest of the two and a great default if you're in Europe or don't mind a EU/US-East region.</p>

        <ul>
            <li><strong>Server type:</strong> <code>CX22</code> &mdash; 2 vCPU shared, 4GB RAM, 40GB disk, <strong>~4&ndash;5 EUR/month</strong> (~$5, including the IPv4 surcharge; <em>approximate</em> &mdash; check current pricing). The <code>CAX</code> (ARM) line is cheaper still, and Bun runs natively on arm64.</li>
            <li><strong>Location:</strong> nearest region &mdash; Falkenstein / Nuremberg (DE), Helsinki (FI), Ashburn VA / Hillsboro OR (US).</li>
            <li><strong>Image:</strong> Ubuntu 24.04. Add your SSH key.</li>
        </ul>

        <p>Create &rarr; wait ~30 seconds &rarr; <code>ssh root@&lt;ip&gt;</code> &rarr; continue from <a href="#the-flow">step 3</a> above.</p>
    </section>

    <section class="section" id="digitalocean">
        <h2>DigitalOcean — worked example</h2>

        <p>DigitalOcean (<a href="https://cloud.digitalocean.com" target="_blank" rel="noopener">cloud.digitalocean.com</a>) is the most GUI-friendly option, US-centric, and slightly pricier.</p>

        <p><strong>One-command shortcut:</strong> on a fresh DigitalOcean droplet, <code>curl -fsSL https://parachute.computer/install/digitalocean.sh | bash</code> does steps 3&ndash;4 for you (Bun + hub + vault + <code>init</code>). The dedicated <a href="/deploy/digitalocean/">Deploy to DigitalOcean</a> guide walks the full droplet-to-connected-AI path. The manual steps below work on any provider.</p>

        <ul>
            <li><strong>Droplet:</strong> Basic / Regular shared CPU. <strong>2GB / 1 vCPU / 50GB at ~$12/month</strong> is the comfortable floor (1GB at ~$6/month runs hub + vault but is tight with surface + scribe). <em>Approximate.</em></li>
            <li><strong>Region:</strong> nearest you.</li>
            <li><strong>Image:</strong> Ubuntu 24.04 x64.</li>
            <li><strong>Authentication:</strong> SSH Key.</li>
        </ul>

        <p>Create &rarr; <code>ssh root@&lt;ip&gt;</code> &rarr; continue from <a href="#the-flow">step 3</a> above.</p>
    </section>

    <section class="section">
        <h2>Expose to the internet</h2>

        <p>On an SSH'd server, <code>parachute init</code> pre-selects public exposure for you. For a clean public HTTPS URL &mdash; the kind the claude.ai connector wants:</p>

        <div class="code-inline">
            <code>parachute expose public --cloudflare --domain vault.example.com</code>
        </div>

        <p>The apex (<code>example.com</code>) must already be a Cloudflare zone (the free tier is fine). <strong>Expose <em>creates</em> the tunnel</strong> &mdash; don't pre-create it. Each machine gets its own per-hostname tunnel name (no account-wide collision); override with <code>--tunnel-name</code> only to deliberately reuse one. Run the command interactively to be prompted for the hostname.</p>

        <p>The <code>cloudflared</code> connector installs as its own managed unit, so the tunnel returns automatically after a reboot.</p>

        <aside class="subtle-note">
            <p>Honest caveats: Cloudflare needs a domain you own. The only domain-free public path is a Tailscale Funnel &mdash; if <a href="https://tailscale.com" target="_blank" rel="noopener">Tailscale</a> is set up on the box, <code>parachute expose public</code> without <code>--cloudflare</code> gives you a <code>*.ts.net</code> URL. Public exposure is marked <em>exploratory</em> relative to the more-hardened tailnet path &mdash; but Cloudflare public HTTPS is what works with claude.ai today.</p>
        </aside>
    </section>

    <section class="section">
        <h2>Pitfalls</h2>

        <p>Most of these you'll never hit. A few are common enough &mdash; the Cloudflare bot-protection one especially &mdash; to be worth reading before you wire up the Claude connector.</p>

        <aside class="upgrade-callout">
            <strong>Cloudflare Bot Fight Mode breaks the Claude connector.</strong>
            <p>The symptom is sneaky: consent passes in your browser, then the integration is "rejected the credentials." Under the hood, Claude's server-to-server token exchange gets a 403. Fix it in <strong>Cloudflare &rarr; Security &rarr; Bots</strong>: turn off Bot Fight Mode / Super Bot Fight Mode / Browser Integrity Check for the zone, or add a WAF Skip rule for <code>/oauth/*</code>, <code>/.well-known/*</code>, and <code>/vault/*</code>. Also remove any Cloudflare Access policy on the host. Confirm the fix with <code>curl -A python-requests https://&lt;host&gt;/.well-known/oauth-authorization-server</code>.</p>
        </aside>

        <ul>
            <li><strong>Bun 1.3+ required.</strong> 1.2.x produces a cosmetic <code>InvalidPackageResolution</code> warning. Run in a <em>fresh</em> shell so <code>~/.bun/bin</code> is on your PATH.</li>
            <li><strong>Non-root reboot survival needs linger.</strong> If you installed as a non-root user, run <code>sudo loginctl enable-linger $USER</code> so the user unit starts at boot. A root install is a system unit and sidesteps this.</li>
            <li><strong>Ports 1939 (hub) / 1940 (vault behind it).</strong> Don't expose 1940 &mdash; clients use the hub origin + <code>/mcp</code>. With a Cloudflare Tunnel you don't open 1939 or 1940 publicly at all.</li>
            <li><strong>RAM headroom.</strong> ~512MB fits the stack at sub-1000 notes; 2GB is comfortable with surface + scribe.</li>
            <li><strong>Keep current.</strong> <code>parachute upgrade</code> (add <code>--channel rc</code> for release candidates). DB migrations run on the first post-upgrade boot.</li>
        </ul>
    </section>

    <section class="section">
        <h2>What you get</h2>

        <p>One <code>systemd</code> unit running the hub on :1939 &mdash; with reboot survival and admin-UI module management. The hub supervises vault, surface (with <a href="https://github.com/ParachuteComputer/parachute-surface/tree/main/packages/notes-ui" target="_blank">Notes</a> auto-bootstrapped as its first hosted surface), and <a href="https://github.com/ParachuteComputer/parachute-scribe" target="_blank">Scribe</a> as child processes. Everything lives under <code>~/.parachute/</code> on the box's own disk &mdash; you fully own it. See the <a href="/design/2026-05-18-v06-deploy-architecture/">v0.6 deploy architecture note</a> for the full shape.</p>
    </section>

    <section class="section">
        <h2>Cost</h2>

        <p>All figures <em>approximate</em> &mdash; check current provider pricing.</p>

        <ul>
            <li><strong>Hetzner CX22:</strong> ~4&ndash;5 EUR/month (~$5), including IPv4. The ARM <code>CAX</code> line is cheaper.</li>
            <li><strong>DigitalOcean:</strong> ~$12/month for the comfortable 2GB droplet (~$6/month for 1GB, tighter).</li>
            <li><strong>Domain + Cloudflare:</strong> the Cloudflare zone is free; you only pay for the domain you already own. TLS is included via the tunnel.</li>
        </ul>

        <p><strong>Expected total:</strong> roughly $5&ndash;12/month for hub + vault + surface (with notes-ui auto-bootstrapped) + scribe running together, depending on provider and instance size.</p>

        <p><em>Compare locally:</em> the same stack on your own Mac or Linux box is free, but you give up the always-on URL and the "works from my phone, anywhere" property. The VPS path is the trade.</p>
    </section>

    <section class="section">
        <h2>Why a VPS over a container PaaS</h2>

        <p>The bun-native path is our <strong>tested, exercised happy path</strong> &mdash; the same commands we run on laptops, in CI, and in every install doc.</p>

        <p>A container PaaS (<a href="/deploy/render/">Render's <code>render.yaml</code></a>, <a href="/deploy/fly/">Fly's Dockerfile</a>) is a genuinely supported peer, and great for one-click GUI ops or portable volume snapshots. But its build <em>diverges</em> from that happy path &mdash; image <code>CMD</code> boot, modules onto a mounted disk, disk-vs-image upgrade semantics &mdash; code the laptop and VPS paths don't exercise.</p>

        <p>On a VPS you get the exact architecture that runs on a laptop (hub-as-supervisor under <code>systemd</code>, modules as children sharing <code>~/.parachute/</code>) with reboot survival and admin-UI module management, on a box that never sleeps and that you fully own. If you'd rather click than SSH, <a href="/deploy/render/">Deploy to Render</a> and <a href="/deploy/fly/">Deploy to Fly.io</a> are the container alternative.</p>
    </section>

    <section class="section">
        <h2>What comes next</h2>

        <ul>
            <li><a href="/deploy/digitalocean/">Deploy to DigitalOcean</a> &mdash; the one-command <code>curl … | bash</code> version of this flow on a $6/mo droplet.</li>
            <li><a href="/start/">Local install</a> &mdash; the same stack on your own machine. Free, but sleeps when your laptop sleeps.</li>
            <li><a href="/deploy/render/">Deploy to Render</a> and <a href="/deploy/fly/">Deploy to Fly.io</a> &mdash; the container PaaS paths, if you'd rather click than SSH.</li>
            <li><a href="/design/2026-05-18-v06-deploy-architecture/">v0.6 deploy architecture</a> &mdash; the design note explaining <em>why</em> it's hub-as-supervisor.</li>
            <li><a href="https://github.com/ParachuteComputer/parachute-hub/issues" target="_blank">parachute-hub issues</a> &mdash; deploy questions, bugs, friction. We read all of them.</li>
        </ul>
    </section>

    <section class="cta">
        <h2>A box that never sleeps</h2>
        <div class="code-inline" style="margin: 1.5rem auto; max-width: 640px;">
            <code>bun add -g @openparachute/hub &amp;&amp; parachute init</code>
        </div>
        <p class="cta-secondary">Or <a href="/start/">install locally</a> first.</p>
    </section>

</main>
