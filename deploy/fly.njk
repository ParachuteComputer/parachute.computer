---
layout: base.njk
title: "Deploy to Fly.io"
description: "Deploy Parachute to Fly.io. Your own hub, your own data, on a portable container substrate. About five minutes from clone to a running URL."
permalink: /deploy/fly/
---
<main>

    <section class="hero">
        <h1 class="fade-up fade-up-2">Deploy to Fly.io</h1>
        <p class="tagline fade-up fade-up-3">Your own Parachute on Fly.io. CLI-first deploy, portable volume snapshots, ~$3.50/month. About five minutes from clone to a running URL.</p>
    </section>

    <aside class="upgrade-callout">
        <strong>A legacy path &mdash; not the recommended way to self-host.</strong>
        <p>Fly runs Parachute as a container, which diverges from the bun-native <code>parachute init</code> install the rest of the product is built around. We hit container-specific friction often enough that it's no longer the path we point people to. The always-on path we recommend now is <a href="/deploy/vps/"><strong>Deploy to a VPS</strong></a> (Hetzner, DigitalOcean, or any Linux box) &mdash; the same one-command install, on a box that never sleeps. This guide stays for operators already running on Fly.</p>
    </aside>

    <section class="section">
        <h2>One command to start</h2>

        <p><strong>Why this path.</strong> Parachute is happiest on your own machine, but a laptop sleeps. Fly.io is a container-based alternative that still lets you own everything &mdash; a single container running your hub, with a persistent volume for your vault, all in your Fly account. No tenancy, no shared database, no Parachute-operated cloud sitting between you and your data. It's not the path we recommend &mdash; <a href="/deploy/vps/">Deploy to a VPS</a> is the one we point people to &mdash; but if you're already on Fly or prefer a container, the full walkthrough is below.</p>

        <p><strong>Same setup as every install.</strong> A Fly deploy isn't a different onboarding &mdash; it's the same <a href="/start/">unified flow</a>. Locally you'd run <code>parachute init</code> to get a hub and land in the setup wizard; here, Fly gives you a running, already-exposed hub at a <code>*.fly.dev</code> URL, and you land in the <em>identical</em> wizard at <code>/admin/setup</code>. You'll walk the same Account &rarr; Vault steps an operator on a laptop or an EC2 box walks; the wizard's <em>Expose</em> step auto-skips on Fly (the platform already owns the public URL via <code>*.fly.dev</code>), so you go straight to <em>Done</em>. The only Fly-specific bit is grabbing a one-time bootstrap token from the logs to claim admin (step 3).</p>

        <div class="code-inline">
            <code>gh repo fork ParachuteComputer/parachute-hub --clone && cd parachute-hub<br>./scripts/deploy-to-fly.sh</code>
        </div>

        <p>The script installs <code>flyctl</code> if you don't have it, signs you in if needed, and runs <code>fly launch --copy-config</code> against the repo's <a href="https://github.com/ParachuteComputer/parachute-hub/blob/main/fly.toml" target="_blank"><code>fly.toml</code></a>. First deploy takes about one to two minutes.</p>
    </section>

    <section class="section">
        <h2>What you get</h2>

        <p>One <a href="https://fly.io/docs/about/pricing/" target="_blank">Fly machine</a> (<code>shared-cpu-1x</code>, 512MB RAM, ~$3.34/month all-in for compute + volume in <code>iad</code>) running <strong>hub-as-supervisor</strong> &mdash; the same architecture you'd run locally, just inside a Fly container. One persistent volume mounted at <code>/parachute</code> for module installs and vault data. HTTPS and a <code>*.fly.dev</code> subdomain included; custom domains attach with one <code>fly certs add</code> command.</p>

        <p><strong>Modules are not pre-installed.</strong> The container ships with only the hub. After first boot, the admin UI lets you install <a href="https://github.com/ParachuteComputer/parachute-vault" target="_blank">Vault</a>, <strong>Surface</strong> (the UI host module, with <a href="https://github.com/ParachuteComputer/parachute-surface/tree/main/packages/notes-ui" target="_blank">Notes</a> auto-bootstrapped as the first hosted surface), and <a href="https://github.com/ParachuteComputer/parachute-scribe" target="_blank">Scribe</a> with one click each &mdash; they install onto the persistent volume under <code>/parachute/modules</code>, so they survive redeploys.</p>

        <p>This is the deploy mirror of the local model: locally, the hub supervises vault, surface, and scribe as child processes sharing <code>~/.parachute/</code>. On Fly, the hub supervises the same children in the same container, sharing the mounted volume. See the <a href="/design/2026-05-18-v06-deploy-architecture/">v0.6 deploy architecture note</a> for the full shape.</p>

        <p><strong>Fly vs Render?</strong> Both are container-based alternatives, and both work. Fly is CLI-first (you'll spend more time in a terminal); Render is GUI-first (more clicks, no CLI install needed). Fly costs less (~$3.50 vs $7), and its volumes are <em>portable</em> &mdash; <code>fly volumes snapshot</code> lets you take your data with you, where Render's disks don't snapshot-export. Between the two containers, pick whichever fits your operator preferences &mdash; though for a fresh always-on setup, the path we recommend is <a href="/deploy/vps/">Deploy to a VPS</a>. See <a href="/design/2026-05-26-fly-migration-path/">the Fly migration design note</a> for the longer reasoning.</p>
    </section>

    <section class="section">
        <h2>1. Install flyctl + sign in</h2>

        <p><strong>Why this step.</strong> Fly's deploy flow is CLI-driven. There's no equivalent of Render's "Deploy" button &mdash; instead, <code>flyctl</code> (their CLI) talks to Fly's API to provision the app, machine, and volume.</p>

        <p>If you don't already have flyctl, the deploy script offers to install it for you via Fly's official one-liner. Or install it yourself first:</p>

        <div class="code-inline">
            <code># macOS / Linux<br>curl -L https://fly.io/install.sh | sh<br><br># Windows<br>iwr https://fly.io/install.ps1 -useb | iex</code>
        </div>

        <p>Then sign in:</p>

        <div class="code-inline">
            <code>fly auth login</code>
        </div>

        <p>This opens your browser to Fly's sign-up / sign-in. Free account, no card required to make one (a card is required before you deploy a paid app, which is everything but the smallest free-tier shapes).</p>

        <p><strong>What you should see.</strong> <code>flyctl</code> on your <code>PATH</code>. <code>fly auth whoami</code> prints your email.</p>
    </section>

    <section class="section">
        <h2>2. Fork + clone + deploy</h2>

        <p><strong>Why this step.</strong> Fly deploys from a local checkout, not from a GitHub-connected pipeline by default. Fork the repo so future updates can be pulled in cleanly; clone locally so <code>fly launch</code> has the <code>fly.toml</code> + Dockerfile to work from.</p>

        <div class="code-inline">
            <code>gh repo fork ParachuteComputer/parachute-hub --clone<br>cd parachute-hub<br>./scripts/deploy-to-fly.sh</code>
        </div>

        <p>The script:</p>
        <ol>
            <li>Checks for <code>flyctl</code> (skips if you already installed it in step 1).</li>
            <li>Confirms you're signed in.</li>
            <li>Runs <code>fly launch --copy-config --yes</code>. Fly generates a unique app name (or use <code>./scripts/deploy-to-fly.sh my-app-name</code> to pick your own), provisions a <code>shared-cpu-1x</code> 512MB machine in <code>iad</code>, attaches a 1GB volume at <code>/parachute</code>, and kicks off the build.</li>
            <li>Prints your hub's URL when done.</li>
        </ol>

        <p><strong>Region.</strong> The default is <code>iad</code> (Virginia, US East) &mdash; cheapest tier. To pick a different one, edit <code>primary_region</code> in <code>fly.toml</code> before running the script. Other common picks: <code>sjc</code> (San Jose), <code>lax</code> (Los Angeles), <code>lhr</code> (London), <code>fra</code> (Frankfurt), <code>syd</code> (Sydney). The full list is at <a href="https://fly.io/docs/reference/regions/" target="_blank">Fly's regions docs</a>.</p>

        <p><strong>What you should see.</strong> Build log streaming. After ~60&ndash;100 seconds, "Deployment complete" and a URL like <code>https://your-app-name.fly.dev</code>.</p>
    </section>

    <section class="section">
        <h2>3. Grab the bootstrap token and open setup</h2>

        <p><strong>Why this step.</strong> Hub generates a one-time bootstrap token on first boot and prints it in the startup logs. You'll grab it from <code>fly logs</code> and paste it into the setup wizard to claim admin.</p>

        <div class="code-inline">
            <code>fly logs --app your-app-name --no-tail | grep parachute-bootstrap-</code>
        </div>

        <p>Look for a banner like:</p>

        <div class="code-inline">
<pre><code>[wizard] ════════════════════════════════════════════════════════════════
[wizard]   PARACHUTE BOOTSTRAP TOKEN
[wizard] ════════════════════════════════════════════════════════════════
[wizard]
[wizard]   parachute-bootstrap-XXXXXXXXXXXXX
[wizard]
[wizard]   &rarr; Visit https://YOUR-URL/admin/setup ...</code></pre>
        </div>

        <p>Copy the <code>parachute-bootstrap-XXXXXXXXXXXXX</code> line (prefix + 43-char body). <strong>That's your token.</strong></p>

        <p><strong>Visit your hub's URL</strong> (printed by the deploy script, or run <code>fly status --app your-app-name</code> to find it). You'll land at <code>/admin/setup</code>. Paste the token, choose your admin username + password, click <strong>Create Admin</strong>. You're in.</p>

        <p><strong>The token expires</strong> when admin is created OR when the hub machine restarts &mdash; so don't navigate away mid-setup. If you lose it, redeploy with <code>fly deploy --app your-app-name</code> and grab the fresh token from the new boot's logs.</p>

        <p><strong>What you should see.</strong> The setup wizard, now past the <em>Account</em> step (your admin's been created) and ready to walk you through the rest: <em>Vault</em>, <em>Expose</em>, <em>Done</em>. Step 4 covers the remaining wizard screens.</p>

        <aside class="upgrade-callout">
            <strong>For automated deploys.</strong>
            <p>If you're scripting Fly deploys (CI, custom orchestration, etc.) you can still seed the admin via env vars instead: <code>fly secrets set PARACHUTE_INITIAL_ADMIN_USERNAME=&lt;you&gt; PARACHUTE_INITIAL_ADMIN_PASSWORD=&lt;pw&gt; --app your-app-name</code>. Hub honors them on first boot &mdash; no wizard step needed; you sign in directly at <code>/admin/login</code>. Trade-off: secrets live in Fly's secret store (operator-visible, rotatable via the same command), and rotation triggers a machine restart. The bootstrap-token flow above is the recommended path for hand-deploys.</p>
        </aside>
    </section>

    <section class="section">
        <h2>4. Walk the first-boot wizard</h2>

        <p><strong>Why this step.</strong> Your admin account was created in step 3 via the bootstrap-token form. From here it's the <em>same wizard every Parachute install uses</em> &mdash; the one a laptop operator reaches via <code>parachute init</code>, just pre-advanced past the account step. It walks through what still needs to be set up once and can't be defaulted: your first vault and how this hub is reached.</p>

        <ol>
            <li><strong>Vault.</strong> <em>Create</em> a fresh vault (defaults to <code>default</code> &mdash; any short name of lowercase letters, numbers, hyphens, or underscores works: <code>notes</code>, <code>journal</code>, <code>work-2026</code>, &hellip;), <em>import</em> one from a git repo (a previously-exported Parachute vault on any HTTPS / SSH remote; PAT optional for private repos), or <em>skip</em> and create one later from the admin UI. You can add more vaults later either way; the name shows up in the URL for that vault's API + MCP endpoints (e.g. <code>/vault/notes/mcp</code>).</li>
            <li><strong>Expose.</strong> Hub auto-skips this step on Fly &mdash; the platform owns the public URL via <code>*.fly.dev</code>, so the three radio choices (localhost, tailnet, public-with-custom-domain) don't speak to your setup. You'll see a brief confirmation and move straight to <em>Done</em>.</li>
            <li><strong>Done.</strong> Two tiles: <strong>Open the admin UI</strong> (lands on <code>/admin/vaults</code>; from there you can navigate to <code>/admin/modules</code> to install Notes, Scribe, and any other modules) and <strong>Connect Claude Code (MCP)</strong> (the install command for the MCP wiring &mdash; see step 6).</li>
        </ol>

        <p><strong>What you should see.</strong> The done screen renders the <code>claude mcp add</code> command pre-filled with your hub's URL, vault name, <em>and</em> the auth header &mdash; the wizard auto-mints a fresh hub token scoped to your vault and drops it into the <code>Authorization: Bearer</code> flag for you. Copy-run it as-is. Step 6 walks the command and the fallback if you need to mint a token by hand.</p>
    </section>

    <section class="section">
        <h2>5. Install modules from the admin UI</h2>

        <p><strong>Why this step.</strong> The hub on its own is just the supervisor &mdash; useful for OAuth and module lifecycle, but not yet the thing you want to talk to. Modules are where the actual reading, writing, and capturing happens.</p>

        <p>From the done screen's <strong>Open the admin UI</strong> tile (lands on <code>/admin/vaults</code>), navigate to <strong>Modules</strong> &mdash; that's <code>/admin/modules</code>. You'll see Vault, Surface, and Scribe listed as available (three modules, not four &mdash; Notes ships bundled inside Surface). Click <strong>Install</strong> next to each one you want. Install takes about a minute per module &mdash; the hub runs <code>bun install</code> against the package and persists it under <code>/parachute/modules/node_modules/</code>, then spawns it as a child process and mounts its routes under <code>/vault</code>, <code>/surface</code>, <code>/scribe</code>. Installing Surface auto-bootstraps the Notes UI as its first hosted surface.</p>

        <p><strong>What you should see.</strong> Each installed module switches to a <strong>Running</strong> badge with its mounted path. Notes is reachable at <code>&lt;your-hub&gt;/surface/notes/</code> (the legacy <code>&lt;your-hub&gt;/notes/</code> URL still resolves via hub's redirect), vault's REST API at <code>&lt;your-hub&gt;/vault/default</code>, scribe at <code>&lt;your-hub&gt;/scribe</code>.</p>
    </section>

    <section class="section">
        <h2>6. Connect Claude Code</h2>

        <p><strong>Why this step.</strong> The vault is running and reachable, but no client is wired to it yet. Claude Code talks MCP over HTTP &mdash; one command registers the server in <code>~/.claude.json</code>, then it picks it up on the next session.</p>

        <p>The simplest path: copy the <code>claude mcp add</code> command straight off the wizard's done screen and run it. The wizard already auto-minted a hub token scoped to your vault and pre-filled the <code>Authorization: Bearer</code> header &mdash; the command is complete as shown. On a hosted deploy there's no local browser for the OAuth redirect, so you attach the token explicitly here (unlike a laptop install, where Claude Code completes the browser sign-in for you):</p>

        <div class="code-inline">
            <code>claude mcp add --transport http parachute-default https://your-app-name.fly.dev/vault/default/mcp --header "Authorization: Bearer &lt;hub-JWT&gt;"</code>
        </div>

        <p>If you closed the done screen or want to mint your own token, either source works &mdash; both return a hub JWT (not a legacy <code>pvt_</code> string; vault no longer issues those):</p>

        <ul>
            <li><strong>Admin UI &rarr; Tokens</strong> &mdash; go to <code>/admin/tokens</code>, create a token scoped to your vault (default <code>vault:default:write</code> &mdash; replace <code>default</code> with your vault name if you chose a different one), and copy the JWT it returns.</li>
            <li><strong>On the hub host:</strong> <code>parachute auth mint-token --scope vault:default:write</code> (replace <code>default</code> with your vault name if you chose a different one) prints a JWT to stdout (default 90-day lifetime).</li>
        </ul>

        <p>Then re-run <code>claude mcp add</code> with that JWT in the header flag (or edit the entry in <code>~/.claude.json</code> directly). Start a new Claude Code session in any project, run <code>/mcp</code> &mdash; <code>parachute-default</code> should appear with its nine tools resolved. Memory now persists across every session, backed by your Fly-hosted hub.</p>

        <p><strong>What you should see.</strong> <code>parachute-default</code> in <code>/mcp</code>'s list with status <em>connected</em>. Asking Claude to write a note succeeds and the note appears in the Notes UI on <code>&lt;your-hub&gt;/surface/notes/</code> immediately.</p>
    </section>

    <section class="section" id="custom-domain">
        <h2>Custom domain (optional)</h2>

        <p><strong>Why this step.</strong> The <code>*.fly.dev</code> subdomain works forever, but most people want their own domain &mdash; both for memorability and because the OAuth tokens the hub mints are bound to a canonical origin you'd rather control.</p>

        <ol>
            <li><strong>Attach the domain.</strong> <code>fly certs add your-domain.example.com --app your-app-name</code>. Fly returns the DNS records you need to add.</li>
            <li><strong>Add DNS records.</strong> Point a CNAME (or A + AAAA records, per Fly's instructions) from your subdomain to your app. Wait for propagation (usually under a minute).</li>
            <li><strong>Fly auto-provisions TLS.</strong> Once DNS resolves, Fly fetches a Let's Encrypt certificate. <code>fly certs show your-domain.example.com --app your-app-name</code> reports the status.</li>
            <li><strong>Pin the issuer.</strong> <code>fly secrets set PARACHUTE_HUB_ORIGIN=https://your-domain.example.com --app your-app-name</code>. The machine restarts; tokens minted after this are bound to the new origin.</li>
        </ol>

        <aside class="upgrade-callout">
            <strong>Why <code>PARACHUTE_HUB_ORIGIN</code> matters (and why you can ignore it without one).</strong>
            <p>Hub stamps an "issuer" URL into every OAuth token it mints. By default (variable unset), the issuer is derived from <code>FLY_APP_NAME</code> as <code>https://&lt;app&gt;.fly.dev</code>, so on a vanilla Fly deploy, tokens are minted against your <code>https://your-app-name.fly.dev</code> URL and everything works. <em>The variable only exists for the custom-domain case.</em> Pinning it to your canonical domain means tokens are bound to that domain from day one; if you flip the issuer later (e.g. you set a custom domain post-deploy &mdash; <em>adding</em> or <em>changing</em> <code>PARACHUTE_HUB_ORIGIN</code> both switch the issuer from the auto-derived <code>fly.dev</code> URL to your pinned value), every token minted before the switch becomes invalid and every connected client (Claude Code, Notes PWA, anything else holding a token) has to reconnect through the OAuth flow. Pick the domain you want before you wire up clients, then set the var once and don't churn.</p>
        </aside>
    </section>

    <section class="section">
        <h2>Cost</h2>

        <ul>
            <li><strong>Machine:</strong> ~$3.19/month for <code>shared-cpu-1x</code> 512MB always-on in <code>iad</code>. Other regions are slightly more (<code>sjc</code> ~$3.89, <code>lax</code> ~$4.05). Pricing varies; see <a href="https://fly.io/docs/about/pricing/" target="_blank">Fly's pricing</a> for current rates.</li>
            <li><strong>Volume:</strong> $0.15/GB/month. The 1GB default volume is $0.15/month. Vault databases are tiny; typical single-operator usage stays well under 1GB through the first year.</li>
            <li><strong>Snapshots:</strong> automatic daily snapshots, 5-day retention, free up to 10GB allowance &mdash; effectively free for single-operator usage.</li>
            <li><strong>Custom domain:</strong> free, assuming you already own the domain. TLS via Let's Encrypt is included.</li>
        </ul>

        <p><strong>Expected total:</strong> ~$3.34/month for hub + vault + surface (with notes-ui auto-bootstrapped) + scribe running together in <code>iad</code>. Other regions add ~$0.50&ndash;$1/month.</p>

        <p><em>Compare locally:</em> the same stack on your own Mac or Linux box is free, but you give up the always-on URL and the "works from my phone, anywhere" property. The Fly path is the trade.</p>

        <p><em>Compare to Render:</em> Fly's ~$3.34 vs Render's $7 flat &mdash; about half. Render's GUI ops are nicer if you don't want a CLI; Fly's volume snapshots are portable if you ever want to move your data elsewhere.</p>
    </section>

    <section class="section">
        <h2>Limitations</h2>

        <ul>
            <li><strong>One machine shares 512MB of RAM.</strong> Fly <code>shared-cpu-1x</code> 512MB is enough for hub + vault + surface + scribe at typical sub-1000-note usage. If you hit the ceiling &mdash; large vaults, heavy transcription workloads &mdash; scale up with <code>fly scale memory 1024 --app your-app-name</code> (triggers a machine restart, ~30s downtime). Or split a module out to its own Fly app later.</li>
            <li><strong>Single-region by default.</strong> The default <code>fly.toml</code> pins one region. Multi-region Parachute would need volume replication, which Fly doesn't do natively for single-writer SQLite workloads. If <code>iad</code> has a bad day, your hub is down until they fix it. Same constraint as Render's regional disks; not a Fly-specific limit.</li>
            <li><strong>CLI install required.</strong> Unlike Render's one-button deploy, you need <code>flyctl</code> on your machine. The deploy script installs it for you if missing, but the dependency is real.</li>
            <li><strong>Module installs take a minute.</strong> Each "Install" click runs a real <code>bun install</code> on the machine against the npm registry. First-time installs of vault or surface take roughly a minute; redeploys re-use the cached volume.</li>
            <li><strong>Redeploys are not zero-downtime.</strong> When you run <code>fly deploy</code> or push to the repo (if you've wired CI), the machine restarts. Expect ~30 seconds where the hub is unreachable. For a single-operator hub this is a non-issue; for teams it's worth knowing.</li>
            <li><strong>Edge cold-start can briefly 502.</strong> Like every PaaS, Fly's edge proxy occasionally takes a few seconds to settle routes to a newly-started machine. Probes during that window may return 502. Once warm, the hub is stable.</li>
        </ul>
    </section>

    <section class="section">
        <h2>What comes next</h2>

        <ul>
            <li><a href="/start/">Local install</a> &mdash; the same stack on your own machine. Free, but sleeps when your laptop sleeps.</li>
            <li><a href="/deploy/render/">Deploy to Render</a> &mdash; the peer hosted path. GUI-first; pick the one that fits.</li>
            <li><a href="/design/2026-05-18-v06-deploy-architecture/">v0.6 deploy architecture</a> &mdash; the design note explaining <em>why</em> it's one container with hub-as-supervisor.</li>
            <li><a href="/design/2026-05-26-fly-migration-path/">Fly migration design note</a> &mdash; the strategic context for why we added Fly as a peer to Render.</li>
            <li><a href="https://github.com/ParachuteComputer/parachute-hub/issues" target="_blank">parachute-hub issues</a> &mdash; deploy questions, bugs, friction. We read all of them.</li>
        </ul>
    </section>

    <section class="cta">
        <p style="margin-bottom: 1.5rem;">
            <a href="https://github.com/ParachuteComputer/parachute-hub" target="_blank" rel="noopener" class="cta-button">
                Fork &amp; deploy on Fly.io
            </a>
        </p>
        <p class="cta-secondary">Or <a href="/deploy/render/">deploy on Render</a>, or <a href="/start/">install locally</a>.</p>
    </section>

</main>
