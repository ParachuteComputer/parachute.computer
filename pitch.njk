---
permalink: /pitch/
---
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Parachute — Investor Deck</title>
    <meta name="robots" content="noindex">
    <meta name="description" content="Parachute — open source personal AI. Two products, one extended mind.">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=DM+Sans:ital,opsz,wght@0,9..40,300;0,9..40,400;0,9..40,500;1,9..40,400&family=JetBrains+Mono:wght@400&display=swap">
    <style>
        :root {
            --bg:           #faf8f4;
            --bg-soft:      #f3f0ea;
            --fg:           #2c2a26;
            --fg-muted:     #6b6860;
            --fg-dim:       #9a9690;
            --accent:       #4a7c59;
            --accent-soft:  rgba(74, 124, 89, 0.08);
            --accent-light: #6a9b77;
            --border:       #e4e0d8;
            --serif: 'Instrument Serif', Georgia, serif;
            --sans:  'DM Sans', system-ui, sans-serif;
            --mono:  'JetBrains Mono', monospace;
            --content-width: 760px;
        }

        *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

        html { scroll-behavior: smooth; }

        body {
            background: var(--bg);
            font-family: var(--sans);
            color: var(--fg);
            -webkit-font-smoothing: antialiased;
            line-height: 1.6;
        }

        /* ── Page header ── */
        .page-header {
            padding: 2.5rem 3rem;
            display: flex;
            align-items: baseline;
            justify-content: space-between;
            border-bottom: 1px solid var(--border);
            position: sticky;
            top: 0;
            background: var(--bg);
            z-index: 100;
        }
        .page-header-logo {
            font-family: var(--serif);
            font-size: 1.1rem;
            color: var(--fg);
            letter-spacing: 0.01em;
        }
        .page-header-meta {
            font-family: var(--mono);
            font-size: 0.65rem;
            color: var(--fg-dim);
            letter-spacing: 0.1em;
            text-transform: uppercase;
        }

        /* ── Content wrapper ── */
        .content {
            max-width: var(--content-width);
            margin: 0 auto;
            padding: 0 2rem;
        }

        /* ── Pitch sections ── */
        .pitch-section {
            padding: 6rem 0 5rem;
            border-bottom: 1px solid var(--border);
        }
        .pitch-section:last-child {
            border-bottom: none;
            padding-bottom: 8rem;
        }

        .section-label {
            font-size: 0.65rem;
            letter-spacing: 0.14em;
            text-transform: uppercase;
            color: var(--accent);
            font-weight: 500;
            margin-bottom: 1.5rem;
            font-family: var(--sans);
        }

        /* The big scannable headline — visible at a glance during presentation */
        .section-headline {
            font-family: var(--serif);
            font-size: clamp(2.4rem, 5vw, 3.75rem);
            font-weight: 400;
            letter-spacing: -0.02em;
            line-height: 1.08;
            color: var(--fg);
            margin-bottom: 2.5rem;
        }
        .section-headline em {
            font-style: italic;
            color: var(--fg-muted);
        }

        /* Supporting content — readable when going deeper */
        .section-body {
            color: var(--fg-muted);
        }

        .section-body p {
            font-size: 1.05rem;
            line-height: 1.75;
            margin-bottom: 1rem;
            max-width: 640px;
        }
        .section-body p:last-child { margin-bottom: 0; }

        .section-body ul {
            list-style: none;
            margin-top: 0.5rem;
        }
        .section-body li {
            padding: 0.55rem 0 0.55rem 1.5rem;
            position: relative;
            font-size: 1rem;
            line-height: 1.6;
            color: var(--fg-muted);
            border-bottom: 1px solid var(--border);
        }
        .section-body li:first-child { border-top: 1px solid var(--border); }
        .section-body li::before {
            content: "›";
            position: absolute;
            left: 0.25rem;
            color: var(--accent-light);
        }
        .section-body li strong { color: var(--fg); font-weight: 500; }

        /* ── Opening — full-bleed ── */
        .opening {
            padding: 8rem 0 7rem;
            border-bottom: 1px solid var(--border);
        }
        .opening-eyebrow {
            font-family: var(--mono);
            font-size: 0.7rem;
            letter-spacing: 0.1em;
            text-transform: uppercase;
            color: var(--fg-dim);
            margin-bottom: 2rem;
        }
        .opening h1 {
            font-family: var(--serif);
            font-size: clamp(3rem, 7vw, 5.5rem);
            font-weight: 400;
            letter-spacing: -0.025em;
            line-height: 1.02;
            color: var(--fg);
            margin-bottom: 2rem;
        }
        .opening-sub {
            font-family: var(--serif);
            font-style: italic;
            font-size: 1.35rem;
            color: var(--fg-muted);
            max-width: 500px;
            line-height: 1.5;
        }

        /* ── Numbered list ── */
        .num-list {
            display: flex;
            flex-direction: column;
            gap: 0;
            margin-top: 0.5rem;
        }
        .num-item {
            display: flex;
            gap: 1.5rem;
            align-items: flex-start;
            padding: 1.25rem 0;
            border-bottom: 1px solid var(--border);
        }
        .num-item:first-child { border-top: 1px solid var(--border); }
        .num {
            font-family: var(--serif);
            font-size: 1.75rem;
            color: var(--accent);
            line-height: 1;
            flex-shrink: 0;
            width: 1.5rem;
            text-align: right;
            padding-top: 0.15rem;
        }
        .num-body strong {
            display: block;
            color: var(--fg);
            font-weight: 500;
            font-size: 1rem;
            margin-bottom: 0.25rem;
        }
        .num-body span {
            font-size: 0.9rem;
            color: var(--fg-muted);
            line-height: 1.55;
        }

        /* ── Product cards ── */
        .product-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
            margin-top: 0.5rem;
        }
        .product-card {
            background: var(--bg-soft);
            border: 1px solid var(--border);
            border-radius: 10px;
            padding: 1.75rem;
        }
        .product-card h3 {
            font-family: var(--serif);
            font-size: 1.3rem;
            font-weight: 400;
            color: var(--fg);
            margin-bottom: 0.4rem;
        }
        .product-card .product-desc {
            font-size: 0.875rem;
            color: var(--fg-muted);
            margin-bottom: 1rem;
            line-height: 1.5;
        }
        .product-card ul { margin-top: 0; }
        .product-card li {
            font-size: 0.875rem;
            padding: 0.4rem 0 0.4rem 1.25rem;
        }
        .product-card li:first-child { border-top: none; }

        /* ── Flywheel ── */
        .flywheel {
            display: flex;
            flex-direction: column;
            margin: 1rem 0 1.5rem;
            max-width: 480px;
        }
        .fw-step {
            font-family: var(--serif);
            font-size: 1.3rem;
            color: var(--fg);
            padding: 0.85rem 0;
            border-bottom: 1px solid var(--border);
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }
        .fw-step:first-child { border-top: 1px solid var(--border); }
        .fw-step.final { color: var(--accent); font-weight: 400; }
        .fw-num {
            font-family: var(--mono);
            font-size: 0.65rem;
            color: var(--fg-dim);
            letter-spacing: 0.06em;
            flex-shrink: 0;
            width: 1.5rem;
        }

        /* ── Stats ── */
        .stats {
            display: flex;
            gap: 0;
            margin: 1rem 0;
            border: 1px solid var(--border);
            border-radius: 10px;
            overflow: hidden;
            background: #fff;
        }
        .stat {
            flex: 1;
            padding: 1.75rem 1.5rem;
            border-right: 1px solid var(--border);
        }
        .stat:last-child { border-right: none; }
        .stat-num {
            font-family: var(--serif);
            font-size: clamp(2.5rem, 5vw, 3.75rem);
            font-weight: 400;
            color: var(--accent);
            line-height: 1;
            letter-spacing: -0.03em;
            margin-bottom: 0.5rem;
        }
        .stat-label {
            font-size: 0.8rem;
            color: var(--fg-muted);
            line-height: 1.4;
        }

        /* ── Comparables ── */
        .comp-table {
            margin-top: 0.5rem;
            border: 1px solid var(--border);
            border-radius: 10px;
            overflow: hidden;
            background: #fff;
        }
        .comp-row {
            display: flex;
            align-items: baseline;
            gap: 1rem;
            padding: 0.9rem 1.25rem;
            border-bottom: 1px solid var(--border);
        }
        .comp-row:last-child { border-bottom: none; }
        .comp-name {
            font-weight: 500;
            color: var(--fg);
            font-size: 0.9rem;
            min-width: 120px;
            flex-shrink: 0;
        }
        .comp-detail {
            font-size: 0.875rem;
            color: var(--fg-muted);
            line-height: 1.4;
        }
        .comp-detail em { font-style: normal; color: var(--accent); font-weight: 500; }

        /* ── Pricing ── */
        .tier-table {
            margin-top: 0.75rem;
            border: 1px solid var(--border);
            border-radius: 10px;
            overflow: hidden;
            background: #fff;
        }
        .tier-row {
            display: flex;
            align-items: baseline;
            gap: 1.25rem;
            padding: 1rem 1.25rem;
            border-bottom: 1px solid var(--border);
        }
        .tier-row:last-child { border-bottom: none; }
        .tier-price {
            font-family: var(--serif);
            font-size: 1.5rem;
            color: var(--accent);
            min-width: 5rem;
            line-height: 1;
            flex-shrink: 0;
        }
        .tier-price span {
            font-family: var(--sans);
            font-size: 0.75rem;
            color: var(--fg-dim);
        }
        .tier-body { font-size: 0.9rem; color: var(--fg-muted); line-height: 1.5; }
        .tier-body strong { color: var(--fg); font-weight: 500; }
        .tier-row-highlight { background: var(--accent-soft); }
        .tier-row-power { background: var(--bg-soft); }
        .tier-row-power .tier-price { color: var(--fg); }

        /* ── Team ── */
        .team-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0;
            margin-top: 0.5rem;
            border: 1px solid var(--border);
            border-radius: 10px;
            overflow: hidden;
            background: #fff;
        }
        .member {
            padding: 1.25rem 1.5rem;
            border-bottom: 1px solid var(--border);
            border-right: 1px solid var(--border);
        }
        .member:nth-child(2n) { border-right: none; }
        .member:nth-last-child(-n+2) { border-bottom: none; }
        .member:last-child:nth-child(odd) {
            border-right: none;
            grid-column: span 2;
        }
        .member strong {
            display: block;
            color: var(--fg);
            font-weight: 500;
            font-size: 0.9rem;
            margin-bottom: 0.25rem;
        }
        .member span { font-size: 0.825rem; color: var(--fg-muted); line-height: 1.45; }

        /* ── Ask ── */
        .ask-box {
            border: 1px solid var(--border);
            border-radius: 10px;
            overflow: hidden;
            background: #fff;
            margin: 0.75rem 0 1rem;
        }
        .ask-row {
            display: flex;
            justify-content: space-between;
            align-items: baseline;
            padding: 1rem 1.4rem;
            border-bottom: 1px solid var(--border);
        }
        .ask-row:last-child { border-bottom: none; }
        .ask-row .lbl { font-size: 0.825rem; color: var(--fg-muted); }
        .ask-row .val { font-family: var(--serif); font-size: 1.1rem; color: var(--fg); }

        .callout {
            background: var(--accent-soft);
            border: 1px solid rgba(74,124,89,0.15);
            border-radius: 8px;
            padding: 1rem 1.25rem;
            font-size: 0.875rem;
            color: var(--fg-muted);
            line-height: 1.6;
            margin-top: 0.75rem;
        }
        .callout strong { color: var(--fg); font-weight: 500; }

        /* ── Close ── */
        .close-links {
            margin-top: 2rem;
            display: flex;
            flex-direction: column;
            gap: 0.4rem;
        }
        .close-links a {
            color: var(--accent);
            text-decoration: none;
            font-size: 0.95rem;
        }
        .close-links a:hover { text-decoration: underline; }

        /* ── Mobile ── */
        @media (max-width: 640px) {
            .page-header { padding: 1.5rem; }
            .content { padding: 0 1.25rem; }
            .opening { padding: 5rem 0 4rem; }
            .pitch-section { padding: 4rem 0 3rem; }
            .product-grid { grid-template-columns: 1fr; }
            .stats { flex-direction: column; }
            .stat { border-right: none; border-bottom: 1px solid var(--border); }
            .stat:last-child { border-bottom: none; }
            .team-grid { grid-template-columns: 1fr; }
            .member { border-right: none; }
            .member:last-child:nth-child(odd) { grid-column: span 1; }
        }
    </style>
</head>
<body>

<header class="page-header">
    <span class="page-header-logo">Parachute</span>
    <span class="page-header-meta">Investor Deck · 2026</span>
</header>

<div class="content">

    <!-- Opening -->
    <div class="opening">
        <div class="opening-eyebrow">Open Parachute, PBC</div>
        <h1>Your AI doesn't<br>know you.</h1>
        <p class="opening-sub">We're building the extended mind that changes that.</p>
    </div>

    <!-- Problem -->
    <section class="pitch-section">
        <div class="section-label">The Problem</div>
        <h2 class="section-headline">Powerful AI.<br>No memory. <em>No trust.</em></h2>
        <div class="section-body">
            <ul>
                <li><strong>Scattered thinking</strong> — voice memos here, typed notes there, AI conversations that vanish. The tools don't talk to each other.</li>
                <li><strong>No continuity</strong> — every AI session starts cold. You re-explain your context every time, and nothing compounds.</li>
                <li><strong>No trust</strong> — real help requires real context, but you wouldn't share your actual thinking with a tool that stores it on someone else's server.</li>
            </ul>
            <p style="margin-top: 1.5rem; font-family: var(--serif); font-style: italic; font-size: 1.15rem; color: var(--fg);">Instead of you learning the system — the system should learn you.</p>
        </div>
    </section>

    <!-- Why Now -->
    <section class="pitch-section">
        <div class="section-label">Why Now</div>
        <h2 class="section-headline">Three things<br>converged.</h2>
        <div class="section-body">
            <div class="num-list">
                <div class="num-item">
                    <span class="num">1</span>
                    <div class="num-body">
                        <strong>Models crossed a threshold</strong>
                        <span>Claude is genuinely useful for personal thinking — not just coding tasks. The quality is there for an extended mind to actually work.</span>
                    </div>
                </div>
                <div class="num-item">
                    <span class="num">2</span>
                    <div class="num-body">
                        <strong>Local-first is viable</strong>
                        <span>On-device voice transcription now works fully offline at production quality — Sherpa-ONNX + Parakeet. No server required. No audio leaving your device.</span>
                    </div>
                </div>
                <div class="num-item">
                    <span class="num">3</span>
                    <div class="num-body">
                        <strong>Privacy is a real concern</strong>
                        <span>Open source + local-first is the only honest answer to "who sees my thinking?" Closed-source AI tools holding your personal context is a liability, not a feature.</span>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Solution -->
    <section class="pitch-section">
        <div class="section-label">The Solution</div>
        <h2 class="section-headline">Two products.<br>One extended mind.</h2>
        <div class="section-body">
            <div class="product-grid">
                <div class="product-card">
                    <h3>Parachute Daily</h3>
                    <p class="product-desc">Journaling with gentle AI. Designed for everyone — no technical setup, no learning curve.</p>
                    <ul>
                        <li>Voice or text, wherever you think</li>
                        <li>Daily reflections, weekly synthesis</li>
                        <li>Pattern detection over time</li>
                        <li>Hosted subscription — works immediately</li>
                    </ul>
                </div>
                <div class="product-card">
                    <h3>Parachute Computer</h3>
                    <p class="product-desc">Full agentic platform for power users who want their AI doing real work.</p>
                    <ul>
                        <li>Brain knowledge graph + Chat</li>
                        <li>Three trust tiers, Docker sandbox</li>
                        <li>Telegram, Discord, Matrix bots</li>
                        <li>Open source — self-hosted or managed</li>
                    </ul>
                </div>
            </div>
            <p style="margin-top: 1.25rem;">Both products share the same graph database. Everything you capture in Daily is instantly available to Computer. No migration, no import&mdash;the data is already there.</p>
        </div>
    </section>

    <!-- Flywheel -->
    <section class="pitch-section">
        <div class="section-label">The Flywheel</div>
        <h2 class="section-headline">Context compounds<br>over time.</h2>
        <div class="section-body">
            <div class="flywheel">
                <div class="fw-step"><span class="fw-num">01</span>Start journaling in Daily</div>
                <div class="fw-step"><span class="fw-num">02</span>Build months of personal context</div>
                <div class="fw-step"><span class="fw-num">03</span>Upgrade to Computer</div>
                <div class="fw-step"><span class="fw-num">04</span>Brain ingests your history instantly</div>
                <div class="fw-step final"><span class="fw-num">→</span>System already knows you</div>
            </div>
            <p>No cold start. No migration. Every day in Daily is an investment that makes Computer smarter from the moment you open it. That's a lock-in that users actually want.</p>
        </div>
    </section>

    <!-- Demand -->
    <section class="pitch-section">
        <div class="section-label">Traction</div>
        <h2 class="section-headline">People are<br>already waiting.</h2>
        <div class="section-body">
            <div class="stats">
                <div class="stat">
                    <div class="stat-num">300+</div>
                    <div class="stat-label">Regen Hub community members ready to onboard at launch</div>
                </div>
                <div class="stat">
                    <div class="stat-num">13</div>
                    <div class="stat-label">Learn Vibe Build cohort — all actively building with AI</div>
                </div>
                <div class="stat">
                    <div class="stat-num">200+</div>
                    <div class="stat-label">PRs merged since January</div>
                </div>
            </div>
            <p>We're intentionally pre-launch — polishing before onboarding rather than churning an early cohort through an unstable product. The community is warm and waiting.</p>
        </div>
    </section>

    <!-- What's Built -->
    <section class="pitch-section">
        <div class="section-label">What Exists Today</div>
        <h2 class="section-headline">This is a<br>working product.</h2>
        <div class="section-body">
            <ul>
                <li><strong>Python/FastAPI server</strong> — full session management, streaming responses, module system, and a CLI for power users</li>
                <li><strong>Flutter app</strong> — macOS, Android, and web; adaptive multi-panel layout that works across devices</li>
                <li><strong>Graph-native memory</strong> — intelligent graph database as core infrastructure, connecting journals, conversations, and entities into one coherent system</li>
                <li><strong>Local voice transcription</strong> — Sherpa-ONNX + Parakeet models, fully offline, no audio ever leaves the device</li>
                <li><strong>Multi-agent teams</strong> — agents that spawn and coordinate sub-agents within your system</li>
                <li><strong>Three bot connectors</strong> — Telegram, Discord, Matrix with per-platform trust levels</li>
            </ul>
        </div>
    </section>

    <!-- Market -->
    <section class="pitch-section">
        <div class="section-label">Market</div>
        <h2 class="section-headline">$16B market.<br>$66/mo problem.</h2>
        <div class="section-body">
            <p>Personal AI platforms are a $16B market today, projected to $268B by 2035 at 41% CAGR. Knowledge workers are paying ~$66/mo fragmented across 4 separate AI tools — and they're tired of it.</p>
            <div class="comp-table" style="margin-top: 1.25rem; margin-bottom: 1.25rem;">
                <div class="comp-row">
                    <div class="comp-name">TwinMind</div>
                    <div class="comp-detail">$5.7M raised at <em>$60M valuation</em>, 30K users. Our most direct comparable — and the benchmark for our cap.</div>
                </div>
                <div class="comp-row">
                    <div class="comp-name">Obsidian</div>
                    <div class="comp-detail">Local-first PKM with ~$25M ARR from sync alone. Proves people pay well for tools they trust with their thinking.</div>
                </div>
                <div class="comp-row">
                    <div class="comp-name">Day One</div>
                    <div class="comp-detail">Journaling at ~$4.8M ARR on $2.92/mo. The floor for what a simple daily habit is worth.</div>
                </div>
                <div class="comp-row">
                    <div class="comp-name">Mem.ai</div>
                    <div class="comp-detail">$28.6M raised (OpenAI fund), widely criticized as stalled. <em>The execution gap is the opportunity.</em></div>
                </div>
            </div>
            <p>67% of knowledge workers rank AI as their most important subscription. 41% report fatigue from managing multiple tools. One system that knows you — and that you can trust — wins.</p>
        </div>
    </section>

    <!-- Business Model -->
    <section class="pitch-section">
        <div class="section-label">Business Model</div>
        <h2 class="section-headline">Start at $1.<br>Grow with every user.</h2>
        <div class="section-body">
            <div class="tier-table">
                <div class="tier-row">
                    <div class="tier-price">$1<span>/mo</span></div>
                    <div class="tier-body"><strong>Sync</strong> — journal on any device, offline-first</div>
                </div>
                <div class="tier-row">
                    <div class="tier-price">$2<span>/mo</span></div>
                    <div class="tier-body"><strong>Daily AI</strong> — sync + daily reflections from your entries</div>
                </div>
                <div class="tier-row tier-row-highlight">
                    <div class="tier-price">$5<span>/mo</span></div>
                    <div class="tier-body"><strong>Advanced AI</strong> — multi-agent, web search, weekly synthesis</div>
                </div>
                <div class="tier-row tier-row-power">
                    <div class="tier-price">$20<span>/mo</span></div>
                    <div class="tier-body"><strong>Hosted Computer</strong> — full agentic platform, no self-hosting required</div>
                </div>
            </div>
            <p style="margin-top: 1.25rem;">vs. $66/mo fragmented across competitors. Open source always free to self-host. Near-term target: $1,000 MRR by April 22.</p>
        </div>
    </section>

    <!-- Team -->
    <section class="pitch-section">
        <div class="section-label">Team</div>
        <h2 class="section-headline">Small, capable,<br>committed.</h2>
        <div class="section-body">
            <div class="team-grid">
                <div class="member">
                    <strong>Aaron Gabriel Neyer</strong>
                    <span>Founder — product vision and architecture. ATLAS, CU Boulder.</span>
                </div>
                <div class="member">
                    <strong>Jon Bo</strong>
                    <span>Parachute Daily co-lead — the voice of simplicity in the product.</span>
                </div>
                <div class="member">
                    <strong>Lucian Hymer</strong>
                    <span>Parachute Computer co-lead — architecture and deep systems.</span>
                </div>
                <div class="member">
                    <strong>Marvin Melzer</strong>
                    <span>Hardware pendant — ATLAS grad, wearable voice capture device.</span>
                </div>
                <div class="member">
                    <strong>Neil Yarnal</strong>
                    <span>Brand & design — gives Parachute its visual identity.</span>
                </div>
            </div>
        </div>
    </section>

    <!-- Ask -->
    <section class="pitch-section">
        <div class="section-label">The Ask</div>
        <h2 class="section-headline">Get in early.<br>Get in at the right terms.</h2>
        <div class="section-body">
            <div class="ask-box">
                <div class="ask-row"><span class="lbl">Instrument</span><span class="val">SAFE (YC standard)</span></div>
                <div class="ask-row"><span class="lbl">Valuation cap</span><span class="val">$5,000,000</span></div>
                <div class="ask-row"><span class="lbl">Check size</span><span class="val">$5k – $50k</span></div>
                <div class="ask-row"><span class="lbl">Use of funds</span><span class="val">Pay team to ship faster</span></div>
            </div>
            <div class="callout">
                The $5M cap is deliberately early-believer pricing. TwinMind — our most direct comparable — raised at <strong>$60M</strong> with 30K users. We're raising before launch to let the right people in at the right moment. Early investors become partners in building this, not just backers.
            </div>
            <p style="margin-top: 1rem; font-size: 0.9rem;">Also applying to CU New Venture Challenge — $144k available to finalists at this same cap.</p>
        </div>
    </section>

    <!-- Close -->
    <section class="pitch-section">
        <div class="section-label">Open Parachute, PBC</div>
        <h2 class="section-headline">The extended mind,<br>open source.</h2>
        <div class="section-body">
            <p>A Colorado Public Benefit Corporation. Open source isn't a distribution strategy here — it's the only honest way to deserve trust with someone's personal thinking. Transparency is the product.</p>
            <div class="close-links">
                <a href="https://github.com/OpenParachutePBC/parachute-computer" target="_blank">github.com/OpenParachutePBC</a>
                <a href="/vision/">parachute.computer/vision</a>
                <a href="/roadmap/">parachute.computer/roadmap</a>
            </div>
        </div>
    </section>

</div><!-- /content -->

</body>
</html>
