---
layout: base.njk
title: "Git backup — back your vault up to a git repository"
description: "Mirror your Parachute vault to a git repo. Every change you save lands as a commit. Push to GitHub for off-machine backup, open the folder in Obsidian, or just keep a local audit trail. Configure end-to-end from the admin UI — no shell required."
permalink: /git-backup/
---
<main>

    <section class="hero">
        <h1 class="fade-up fade-up-2">Back your vault up to git</h1>
        <p class="tagline fade-up fade-up-3">Every change you save lands as a commit. Push to GitHub or keep it local. Configure from the admin page — no shell required.</p>
    </section>

    <section class="section">
        <h2>What git-backup gives you</h2>

        <p>Your vault is a SQLite database. Vault keeps it fast and queryable from MCP, Notes, and any other client. But it's one file — opaque, single-machine, not readable by Obsidian or your terminal, and not directly backupable through git.</p>

        <p>Git-backup adds a <strong>mirror folder</strong> next to the live database. Every note becomes a <code>.md</code> file with frontmatter. Every tag schema becomes a sidecar YAML. Every attachment lives in <code>.parachute/attachments/&lt;id&gt;/</code>. The mirror is a real git repo, so <code>git log</code> shows your edit history, <code>git diff</code> shows what changed between any two points in time, and <code>git push</code> ships an off-machine copy to GitHub (or any git host).</p>

        <p>Three operator-facing wins:</p>
        <ul>
            <li><strong>Audit trail.</strong> Restore any note to any past state by checking out the mirror at that commit. Every save makes a commit you can navigate through.</li>
            <li><strong>Off-machine backup.</strong> Hook a GitHub repo (free private repos are unlimited) and your vault survives a disk failure or moves with you to a new machine.</li>
            <li><strong>Obsidian-readable.</strong> The mirror is portable Markdown. Open the folder in Obsidian, VS Code, or your editor of choice — the files are normal Markdown with normal frontmatter.</li>
        </ul>

        <p>This is one-way (vault &rarr; mirror) projection. Edits in the mirror don't flow back into vault. Bidirectional sync is a deferred design item; today the model is "vault is the runtime, git is the archive." See the <a href="/design/2026-05-20-vault-as-git-projection/">design doc</a> for the architecture and where it's headed.</p>
    </section>

    <section class="section">
        <h2>1. Open the git-backup page</h2>

        <p>Sign in to your hub. Navigate to <strong>Vaults</strong>, click the vault you want to back up, then click <strong>Git backup</strong> under "Manage." The page mounts at <code>/vault/&lt;name&gt;/admin/mirror</code>.</p>

        <p>You'll see a "Status" card at the top (everything blank — that's the first-run state) and a configuration form below. The master switch is off by default; no mirror activity happens until you flip it on.</p>
    </section>

    <section class="section">
        <h2>2. Pick a preset</h2>

        <p>Three preset cards make the common shapes one click. Pick whichever matches what you want:</p>

        <ul>
            <li><strong>History</strong> &mdash; vault manages the mirror at a hidden path (<code>~/.parachute/vault/data/&lt;name&gt;/mirror/</code>). Zero configuration; no GitHub needed. Use this when you want an audit trail and never plan to leave the box. The folder is recreated automatically if it goes missing.</li>
            <li><strong>Live Mirror</strong> &mdash; you pick a visible folder (like <code>~/Documents/my-vault-mirror</code>). Vault keeps it in sync via the event-driven path. Use this when you want to open the folder in Obsidian, push to GitHub, or both.</li>
            <li><strong>Manual Export</strong> &mdash; same as Live Mirror but no auto-sync. You run "Run export now" from the admin page (or <code>parachute-vault export</code> from the CLI) when you want a snapshot. Use this if you prefer batch snapshots over per-edit commits.</li>
        </ul>

        <p>Picking a preset pre-fills the form below. You can still customize anything afterwards.</p>
    </section>

    <section class="section">
        <h2>3. Configure the folder</h2>

        <p>For <strong>History</strong>: nothing to configure. Save the form; the mirror folder is created on first save and re-created if it goes missing.</p>

        <p>For <strong>Live Mirror</strong> or <strong>Manual Export</strong>: pick a path. The folder needs to already exist on disk AND already be a git repo. If you haven't created it yet:</p>

        <pre><code>mkdir -p ~/Documents/my-vault-mirror
cd ~/Documents/my-vault-mirror
git init</code></pre>

        <p>Then paste the full absolute path (e.g. <code>/Users/you/Documents/my-vault-mirror</code> on macOS, <code>/home/you/Documents/my-vault-mirror</code> on Linux) into the External path field. The server doesn't tilde-expand <code>~</code>, so use the resolved form.</p>

        <p>If you don't have shell access, pick <strong>History</strong> — it's the same engine, the folder lives somewhere vault knows how to manage on your behalf, and you can always switch later.</p>
    </section>

    <section class="section">
        <h2>4. Connect a git remote (for GitHub-backed backup)</h2>

        <p>This step is optional. Skip it if you only want a local mirror.</p>

        <p>Below the location field you'll see a "Git remote" section. Two flows to pick from:</p>

        <h3 style="margin-top: 1.5rem;">Connect GitHub (recommended for GitHub users)</h3>

        <p>Click <strong>Connect GitHub</strong>. A modal pops up with a short user code (GitHub's format is <code>XXXX-XXXX</code>) and a link to <code>github.com/login/device</code>. Open that link in any browser (your phone counts), paste the code, and click "Authorize Parachute Vault."</p>

        <p>Once authorized, the modal closes and you'll see a list of your GitHub repos. Click one to pick it as the push target. Or click <strong>Create new private repo</strong> to spin up a fresh private repo named after your vault — vault uses GitHub's API to create it, and the mirror starts pushing immediately.</p>

        <p>Same flow as <code>gh auth login</code>. No callback URL setup, no OAuth app on your end — vault uses Device Flow which authorizes from any device with a browser. Tokens land in <code>~/.parachute/vault/.mirror-credentials.yaml</code> with 0600 perms; they never appear in API responses.</p>

        <h3 style="margin-top: 1.5rem;">Use a Personal Access Token (GitLab, Gitea, Bitbucket, self-hosted git)</h3>

        <p>Click <strong>Use Personal Access Token</strong>. Two fields:</p>

        <ul>
            <li><strong>Token</strong> &mdash; a PAT from your git host with <code>repo</code> (or equivalent) scope. <a href="https://github.com/settings/tokens/new?scopes=repo">Create one on GitHub</a> or follow your provider's docs.</li>
            <li><strong>Remote URL</strong> &mdash; the HTTPS clone URL of your target repo (<code>https://github.com/you/your-mirror.git</code>, <code>https://gitlab.com/you/your-mirror.git</code>, etc.).</li>
        </ul>

        <p>Vault validates by running <code>git ls-remote</code> against the URL with the token. If the probe succeeds, the credentials save and the mirror is wired to push.</p>
    </section>

    <section class="section">
        <h2>5. Turn on push</h2>

        <p>In the configuration form, check <strong>Push after each commit</strong>. The form now shows "Will push to @your-login on GitHub" (or the equivalent for PAT) confirming credentials are wired. Save the form.</p>

        <p>If location is <code>internal</code> (the History preset), the push checkbox is hidden — internal mirrors have no remote, so pushing doesn't make sense. Switch to <code>external</code> if you want pushes.</p>

        <p>Push failures (network blip, rate-limit, expired token) are non-fatal: vault logs a warning and keeps running the watch loop. The next successful push catches up.</p>
    </section>

    <section class="section">
        <h2>6. Verify it works</h2>

        <p>Create a note in your vault — via Notes, via the MCP tools in any AI client, or via the CLI. Within a couple of seconds, the Status card on the admin page updates: <strong>Last export</strong> shows the timestamp and inline note count (something like <code>2026-05-28T03:14:15Z · 1 note</code>), and <strong>Last commit</strong> shows a short SHA.</p>

        <p>Browse to your remote repo on GitHub (or wherever) and you should see the new file with your note's content. Open the local mirror folder in your file browser and you'll see the same.</p>

        <p>Delete a note and you should see the file <strong>removed</strong> in the next commit — git-backup propagates deletions, so the mirror stays in sync with your vault rather than accumulating orphans.</p>
    </section>

    <section class="section">
        <h2>Sync timing</h2>

        <p>"On change" is the default mode. Vault subscribes to in-process events on every note/tag/attachment mutation; the export fires within ~500ms of each save. A safety-net poll (default hourly) catches anything the event path misses.</p>

        <p>If you prefer batched snapshots, switch to <strong>Manual only</strong>. No auto-firing; you run "Run export now" or <code>parachute-vault export</code> when you want a commit. Useful if you'd rather one commit per session than one per save.</p>
    </section>

    <section class="section">
        <h2>Troubleshooting</h2>

        <h3 style="margin-top: 1.5rem;">"Path must exist AND be a git repo"</h3>
        <p>The folder you pointed at doesn't exist yet or isn't initialized as git. <code>mkdir -p &lt;path&gt;; cd &lt;path&gt;; git init</code> from a terminal — or switch to the History preset, which manages the path for you.</p>

        <h3 style="margin-top: 1.5rem;">Push failures repeatedly logged</h3>
        <p>Your token expired, was revoked, or never had the right scope. Click <strong>Disconnect</strong> in the Git remote section and reconnect. For GitHub OAuth, re-running Device Flow mints a fresh token. For PAT, generate a new one with <code>repo</code> scope and paste it.</p>

        <h3 style="margin-top: 1.5rem;">"auto_push: true with location: internal is rejected"</h3>
        <p>Internal mirrors have no remote — pushing doesn't make sense. The admin UI hides the push checkbox in this case; the rejection is the backend's safety net when a config arrives via hand-edited YAML.</p>

        <h3 style="margin-top: 1.5rem;">Old notes' files still in the mirror after delete</h3>
        <p>Vault 0.4.9-rc.5+ propagates deletions. If you're on an older version, the mirror retains stale files indefinitely; upgrade to pick up the orphan-sweep behavior, or run <code>parachute-vault export &lt;path&gt;</code> from the CLI to do a fresh full-tree export.</p>

        <h3 style="margin-top: 1.5rem;">I want to switch hosts (GitHub → GitLab, or local-only → GitHub)</h3>
        <p>Click <strong>Disconnect</strong> to clear the current credentials. Then run whichever connection flow matches the new host. Vault rewrites the mirror's git remote URL on save; the existing commit history travels along.</p>
    </section>

    <section class="section">
        <h2>What's in the file format</h2>

        <p>The mirror uses the same portable-markdown format the <code>parachute-vault export</code> CLI emits. One <code>.md</code> file per note. Frontmatter carries the ID, path, tags, metadata, typed links, and attachment refs. <code>.parachute/schemas/&lt;tag&gt;.yaml</code> stores tag-schema definitions. <code>.parachute/attachments/&lt;id&gt;/&lt;file&gt;</code> stores attachment binaries.</p>

        <p>The format round-trips losslessly: <code>parachute-vault import &lt;path&gt;</code> reconstructs the vault from a mirror. So a git push is a real backup — you can clone the repo on a new machine, run import, and get back to where you were.</p>
    </section>

    <section class="section">
        <h2>Related</h2>

        <ul>
            <li><a href="/design/2026-05-20-vault-as-git-projection/">Vault as git projection — v0.7+ architecture</a> &mdash; the design doc this implements.</li>
            <li><a href="/multi-user/">Multi-user — host your hub for friends and family</a> &mdash; the operator companion for running a hub with more than one user. Each user's mirror config is per-vault.</li>
            <li><a href="/install/">Install Parachute</a> &mdash; if you haven't stood up your hub yet, start here.</li>
        </ul>
    </section>
</main>
